# Dev + testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
anyio>=4.0.0
trio>=0.27.0

# HTTP test client (ASGI transport)
httpx>=0.27.0

# CBV decorator used by example controllers
fastapi-restful>=0.6.0

# SQLAlchemy
aiosqlite>=0.19.0
sqlalchemy[asyncio]>=2.0.43

# SQLModel
sqlmodel>=0.0.30

# Beanie (MongoDB) — pinned set known to work with mongomock-motor.
# beanie 2.1.0 calls list_collection_names(authorizedCollections=True)
# which mongomock 4.3.0 doesn't support — pin to 2.0.x until both
# packages catch up (mongomock issue tracked in mongomock/mongomock#967).
beanie>=2.0.0,<2.1.0
motor>=3.3.0
pymongo>=4.9,<5.0
mongomock-motor>=0.0.36

# Common
email-validator>=2.1.0

